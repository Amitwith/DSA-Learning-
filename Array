#include <iostream>
using namespace std;

// find  Minimum Element 
int main() {
    int n , ans = 0 ;
    cout<< "Enter Size of Elements : " << endl;
    cin >> n;
    int arr[100];
    for( int i = 0; i < n; i++ ){
    cin >> arr[i];
    }
    ans = INT_MAX;
    for(int i = 0; i < n; i++){
    
    if(arr[i]<ans)
    ans = arr[i];
    
    }
    cout << "Minimum Element is : " <<  ans<< endl;

    return 0;
}
